{
  "version": 2,
  "buildCommand": "bunx nx build frontend --configuration=production",
  "outputDirectory": "dist/frontend/browser",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index.ts" }
  ]
}
